apiVersion: v1
kind: Service
metadata:
  labels:
    name: web
  name: mywebapp
spec:
  ports:
  - name: "80"
    port: 8080
    targetPort: 80
  selector:
    name: mywebapp
  #type: NodePort
  type: LoadBalancer